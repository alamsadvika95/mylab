variable "master_instance" {
 description = "The project ID to host the cluster in"
 default   =""
}
variable "region" {
 description = "The project ID to host the cluster in"
 default   =""
}
variable "project_id" {
 description = "The project ID to host the cluster in"
 default   =""
}
variable "preferable_zone" {
 description = "The project ID to host the cluster in"
 default   =""
}